# --- Python ---
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# --- Icarus Verilog / open-source sim ---
*.vvp
*.vcd
*.fst
*.lxt

# --- ModelSim / Questa ---
work/
transcript
*.wlf
*.vstf
wlft*
scripts/compile_error.txt

# --- Yosys generic-synthesis netlists (regenerate with `make synth`) ---
results/*.json

# --- sky130hd-mapped netlists and their sims (regenerate with `make gls`) ---
# Each netlist is tens of thousands of lines of generated gates; the committed
# evidence is results/gls_*.log, not the netlist itself.
results/gls/

# --- SAT working files (regenerate with scripts/prove_pe_output_hold.sh) ---
# Generated mutant, yosys scripts and solver logs; the committed evidence is
# the write-up in docs/VERIFICATION_PLAN.md and results/pe_stimulus_*.log.
results/pe_hold/

# --- Build noise ---
build_project.log

# --- Editors / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
